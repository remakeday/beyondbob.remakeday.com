---
title: "제출 모델 확정(Core Sonnet 5 · NPC Haiku 4.5), 비용 정리, main 머지, 로컬 개발 복귀 / 새벽: IP 신뢰 헤더 수정·공개 범위 정리 / 오전: 테스터7·8·9 휴먼테스트, 칸반 자동 동기화"
author: chungsik
tags: [개발일지]
---

09-16 밤 작업에서 이어진 자정 이후 기록(00:00~00:30). 새벽(00:28~01:11)·오전~낮(09:43~12:39) 기록은 아래 "머지와 로컬 개발 복귀" 다음에 이어 적었다.

### 제출 조합 확인 1판 (사용자 요청)

Core `anthropic:claude-sonnet-5` + NPC `anthropic:claude-haiku-4-5`, 플레이어 gemma4:12b, 5회차 1판(`305d77a6`): 점수 11.7 → 11.7 → 16.0 → 24.8 → 29.8, 멸망 종결, 261.5초로 어제 돌린 판 중 가장 빨랐다. 크래시·폴백·서버 오류 0, NPC 발화 100건에서 메타 표현(유저·AI·게임 등) 0건. planner가 5회차에 `beats` 7개(상한 6)로 한 번 재생성했으나 복구 — 어제 Sonnet 판과 같은 구조화 출력 상한 문제다. 백엔드는 `.env`를 건드리지 않고 프로세스 환경변수로만 전환했다가 곧바로 로컬 설정으로 재기동.

### 기록 — 모델 선정·비용·전환 절차

- **`docs/model_evaluation.md`** — A.19 §2에 조합 확인 판 추가, 판정 절에 "사용자 확정(2026-09-17)" 문구, 변경 이력 행. 빠져 있던 어댑터 스모크 표(세 모델 수정 전·후, temperature·effort 전송)와 selfplay 개발용 로그인 관련 수정(`33bebca`)을 §5에 보강. gemma4 대조군 advisor 재생성 수 3 → 2 정정. `metrics.yml` 1줄 추가(총 17줄)
- **`docs/HANDOFF.md` 맨 위 "제출 모델 구성 — 확정"** — 09-20에 `.env`에 맞출 다섯 줄(키 자리·provider·모델), 재기동 확인, 확인용 1판, 콘솔 지출 한도, ollama 롤백, 운영 관찰 항목. 키 값은 문서에 쓰지 않았다
- **`docs/apiscenario.md` 맨 위 "최종 선정 — 2026-09-17 확정"** — 선정·탈락 이유 표, 판당 예상 비용을 두 판의 하네스 기록(호출 수·입출력 문자) × 단가로 재계산: 사람이 5회차까지 한 판(테스터6 `2338ec9f` + 발화 분류기 보정) **$0.68~0.97(950~1,360원)**, self-play 판 $0.54~0.78. 비용의 87~94%가 Core, 관리자 검사가 Core 입력의 56~68%. 완주 100/300/500판 $68~97 / $204~291 / $340~485(EC2면 $35~65 더함). Sonnet thinking 토큰·재생성·임베딩은 미포함(어댑터 `response.usage` 미기록). 임베딩은 Gemini 무료 키 그대로라 한도 확인이 제출 전 과제

### main 머지와 로컬 개발 복귀

- **머지** — 백엔드 590 passed · `tsc` 클린 · 평가 러너 4종 `--help`·NPC 러너 dry-run 정상 · 헤드리스 로그인 테스트 통과 · 추적 파일에 자격 증명 문자열이 없음을 확인하고 `feat/coherence-chain` 12커밋을 main으로 fast-forward(`6498e4f`). 원격 push는 하지 않았다(원격 main `09c3f23`)
- **키 삭제·로컬 복귀** — 사용자가 `backend/.env`의 Anthropic 키 값을 지웠다(줄은 빈 값으로 남음). 설정이 lru_cache라 백엔드를 재기동해 Core `ollama:gemma4:12b`·NPC `ollama:kanana1.5:8b-q4km`(think off)로 확인, 헤드리스 로그인 통과, 로컬 1회차 self-play 크래시 0(8.8점, 66.9초)

### 새벽 — 배포 보안 점검 1건 (A등급, `d3a54e5` 01:04)

09-16 리뷰에서 파킹한 배포 보안 항목 1건을 배포 전에 닫았다.

- **코드** — 관련 테스트 2개 → 5개
- **절차** — 커밋 기록상 sonnet 구현 → opus 리뷰 Critical 0 → 수정 1회, **593 passed**(09-17 00시대 머지 때 590에서 +3). 이 일지 작성 시점에 재실행은 하지 않았다
- **문서** — 관련 문서와 이 일지의 09-16 이월 항목에 "09-17 수정 완료" 표기

### 새벽 — 공개 범위 정리와 데브로그 공개 게이트 (00:28~01:11)

저장소와 허브가 공개라서, 스포일러·운영 한도 같은 내용이 밖으로 나가지 않게 정리했다.

- **스포일러 식별자 중립화 (`08b593d` 01:04)** — 테스트·검증 산출물 42파일 +95/−95. 게임 DB 관련 경로·DB명 표기와 목 응답의 결말 문장을 중립어로 바꿨다. 실제 DB 연결, 실제 결말 텍스트를 검증하는 코드, 원본 채점 로그는 그대로 뒀다 — 검증 의미가 바뀌면 안 되기 때문이다. `frontend/tests/*.cjs` 7개가 여기에 포함된다
- **문서 공개 범위 (`cae8ca1` 01:04)** — `apiscenario.md`에서 전역 하루 판 수 한도로 역산한 최대 비용 문장을 지우고 "실제 한도 값은 배포 `.env`에서 정하고 문서에 적지 않는다"로 바꿨다(한도 숫자가 공개되면 비용 공격 계산에 쓰일 수 있어서). `HANDOFF.md` 제출 전환 절차에 과잉 사용 방지 한도 복원과 배포 보안 설정 확인 한 줄 추가. `teamprofile.md` 허브 주소 `beyondbob.remakeday.com` → `blog.remakeday.com`(3곳)
- **데브로그 스크립트 (`559ba63` 00:28, `bd8b86d` 01:11)** — `scripts/jekyll-devlog.sh`
  - 어제 섹션도 포스트로 다시 복사한다. 23:45 실행 뒤 자정 전후에 어제 섹션을 보완하는 일이 실제로 생겨서다(09-16 섹션이 그 예). 프롬프트의 metrics 경로 `backend/metrics.yml` → `docs/metrics.yml` 정정
  - 허브 포스트를 그대로 복사하지 않고 `claude -p`로 공개용 재작성: 결말 식별자 중립화, 보안·개발 계정·한도 세부 축약. 금지어가 1건이라도 남거나 재작성본이 원문의 절반 미만이면 포스트를 쓰지 않고 로그만 남긴다
  - 재작성 호출은 빈 임시 폴더에서 훅 없이 실행한다 — 프로젝트 메모리·세션 훅 맥락이 공개 글에 섞여 들어가는 것을 막기 위해서다. `--posts-only DATE...`로 지정 날짜만 재생성(+132/−21)
- **로컬 `.env`** — 01:03 배포 준비로 `FRONTEND_BASE_URL`을 운영 도메인으로 바꿨다(테스터9 F2 기록에서 확인). 이 값 때문에 세션 쿠키에 `Secure`가 붙는다 — 아래 테스터9 F2 참고

### 오전 — 휴먼테스트 테스터7·8·9 (기록만, 코드 수정 없음)

세 명 모두 피드백은 기록 문서에 원문·해석·코드 위치로 남기고 조치는 전부 "미착수"다. 회차별 점수는 DB `nights` 읽기 전용 조회.

| 참가자 | 판(attempt) | 시각 | 결과 | 회차별 총점 | 피드백 | 문서 |
|---|---|---|---|---|---|---|
| 테스터7 | `61acc8ef` (이벤트 606) | 10:08~10:56 | 5회차 clear | 33.5 → 40.8 → 66.9 → 66.9 → 94.2 | F1~F8 | `2026-09-16-tester7/tester7.md` |
| 테스터8 | `7cf7484d` (이벤트 595) | 09:43~10:22 | 5회차 doom(통과 0) | 0.0 → 14.6 → 29.2 → 29.2 → 47.9 | F1~F6 | `2026-09-17-test8/tester8.md` |
| 테스터9 | `e6290e17` | 진행 중 | 1회차 55.4 통과 | 1회차만 확인 | F1~F13 | `2026-09-17-tester9/tester9.md`(미커밋) |

- **테스터7** — 09-16 10:33 후보 `e7aa6a4f`는 1회차 첫 장면에서 멈춘 판(이벤트 15건)이라 표본에서 뺐다. side_effect 셀은 테스터6과 같이 끝까지 0. 새 피드백: F3 NPC 선택 창 2×2 배열, F4 신의 개입 판정 「맞다」와 부연 「확신할 수 없다」 모순, F5 NPC 대화 맥락 단절, F6 「무슨 말을 들었나」 같은 내용 질문에 「맞다」가 붙음, F7 같은 시점 다음 그림 강조, F8 상황 이미지를 배경급으로 확대(적용 후보 4안, 협의 대기)
- **테스터8** — 근거 탭 0개로 직접 쓴 제출문만으로 채점됐고 cause·side_effect는 끝까지 0. F1 하루가 지날 때마다 화면이 아래로 밀림, F2 동기 파악 어려움, F3 3회차 아침 전까지 맥락 파악 어려움, F4 주제를 아는 개발자도 40%대(최종 47.9와 일치), F5 민석·은상에게 한 질문을 채연의 질문으로 인식, F6 규칙 해석 실패 시 다른 규칙으로 바꿔 제안
- **테스터9 프론트엔드/UI(10건)** — F1 로그인 안내가 명령조 반말이고 "랜딩"이 개발 용어(`GuardScreen.tsx`), F3 랜딩 버튼 등장(4.2s) 시 BGM 시작·우상단 토글, F4 플레이 안내·단서 기록 한 곳에, F5 채팅창 대신 비주얼노벨 대사창(줄 단위 앞·뒤 이동), F6 장면 그림은 처음에만 보여주고 단서 기록에 보관, F7 단서 기록 카드 → 한 줄 로그, F8 남은 대화 횟수를 HUD 게이지로(최대치 `9 - loop_n`), F9 신의 개입 조언 모아 보기 또는 메모장, F12 대화 완료 인물 회색 처리, F13 장면 설명·규칙 결과·원숭이손 대가 문구가 `scene_execution.py`의 `" ".join(narration)`으로 한 문단에 합쳐짐(DB 이벤트 12225~12235 확인, 대가 문구 2회·대화 2회 차감)
- **테스터9 백엔드/AI(3건)**
  - F2 개발 로그인 후에도 안내 화면 반복 — `backend.log`에서 개발 로그인 200 직후 `POST /sessions` 401 2회. curl로는 로그인 200, 발급 쿠키를 직접 실으면 `/auth/me` 200이라 세션은 유효하고 브라우저가 쿠키를 싣지 않은 상태. 원인 후보는 위 `.env` 변경으로 붙은 `Secure`(http localhost의 Safari 거부) 또는 `127.0.0.1:3500`/`localhost:8500` 호스트 불일치. 설정 변경 없이 사용자 쪽에서 풀렸고 어떤 조치로 풀렸는지 모르므로 원인은 확정하지 않았다
  - F10 「민석의 보고가 채연의 이송으로 이어졌는가?」에 「맞다」 + 본문 「알 수 없다」 — 이벤트 12194·12195 확인. 조언자(gemma4:12b)가 예/아니오 질문을 `lookup`으로 분류했고, 근거 2건의 `supported`가 질문 명제가 아니라 "기록했다"는 하위 사실을 뒷받침했으며, `intervention_interactor.py`는 `lookup`에서 `contradicted`만 `unknown`으로 낮춘다. 테스터7 F4·F6과 같은 계열
  - F11 1회차 55.4(cause 75.0·motive 83.3) — Claude가 공개 관찰만으로 추천한 질문·추리문을 그대로 입력한 "보조 플레이" 표본이다. 처음엔 "채점이 관대하다"고 봤다가 재검토에서 정정했다: 한 문장이 여러 명제를 맞히는 현상은 테스터6 3회차(확정 8건/4문장)·테스터7 5회차(8건/5문장)에도 있고, `cited_chunks`가 모든 명제에 같게 붙는 것은 후보 전체를 기록하는 설계(`night_interactor._judge`)다. 실제 버그는 motive-2의 매칭 문장 지목 오류 하나다. 1회차 점수 비교 테스터6 8.8 · 테스터7 33.5 · 테스터8 0.0 · 테스터9 55.4
- **난이도 판단** — 팀 공통 피드백이 "개발자에게도 너무 어렵다"(테스터8 F3·F4)라서, 보조 플레이에서 통한 행동(본 것 기반 구체 질문, 같은 답만 하는 인물 건너뛰기, 대사와 방송 규칙 잇기, 인과로 잇는 추리문, 새 단서에 신의 질문)을 게임 안 안내·보조 장치로 옮기는 방향으로 정리했다. 채점 엄격도를 조이면 체감 난이도가 더 오르므로 엄격도와 난이도 보조는 목표 점수대를 정한 한 계획에서 함께 다루기로 했다. 채점기 점검은 A등급이라 별도 결정이 필요하다

### 낮 — 허브 칸반 자동 동기화와 자동 커밋·푸시 (`071b500` 12:39)

허브 칸반을 손으로 맞추지 않아도 두 저장소 진행 상황이 따라가게 했다. 3파일 +252/−1.

- **`scripts/jekyll-kanban-sync.sh`(194줄)** — 개발 일지·인계 문서·커밋·파일 구성을 근거로 허브 칸반의 미완료 카드 status만 앞으로 올린다(todo → doing → review → done, 되돌리지 않음). YAML 파싱이 실패하면 전부 원복한다. `--dry-run`(판정만)·`--no-publish`(반영만, 커밋·푸시 안 함). crontab `0 23 * * *` 등록 확인 — 23:45 데브로그보다 먼저 돈다
- **`scripts/beyondbob-publish.sh`(49줄)** — 자동화가 만든 파일만 `git commit --only`로 허브 저장소에 커밋하고 푸시한다. 원격이 앞서면 rebase, 충돌하면 푸시를 멈추고 로컬 커밋만 남긴다. 작업 트리의 다른 수정이 섞이지 않게 하려고 경로 지정 커밋으로 제한했다
- **`jekyll-devlog.sh`** — 게이트를 통과해 새로 쓴 포스트를 위 헬퍼로 커밋·푸시(+10/−1). 허브 쪽 수정 범위도 "자동화 산출물만 자동 커밋·푸시, 세션 수동 수정은 요청 시에만"으로 바뀌었다
- 허브 저장소에 실제로 올라간 칸반·프로필 커밋 내역은 이 일지를 쓰면서 조회 권한이 없어 확인하지 못했다

### 커밋·저장소 상태 (작성 시점)

- **오늘 커밋 13건** — `a815aff` 00:03 ~ `1ea9d81` 12:39. 00:03~00:26 5건(평가·비용 기록과 이 일지), 00:28~01:11 5건(데브로그·보안 점검·스포일러·공개 범위), 12:39 3건(`071b500` 자동화, `dcd6774` 09-16 섹션에 프로덕션 도메인 연결 기록 +10줄, `1ea9d81` 테스터7·8 기록 +179줄)
- **원격** — `main`·`feat/coherence-chain`·`origin/main`이 모두 `1ea9d81`로 같다. 00시대 머지 때 "push 안 함(원격 `09c3f23`)"이었던 상태는 이후 push로 해소됐다
- **미커밋** — `docs/review-verification/2026-09-17-tester9/`(168줄, 테스터9 진행 중) 하나. `git diff --stat`·`--cached --stat` 모두 비어 있음
- **확인하지 못한 것** — 백엔드 테스트 재실행, `localhost:8500/health`·프론트 3500 응답, 허브 저장소 로그는 이 일지 작성 중 명령 승인이 나지 않아 보지 못했다. `backend.log`는 12:40까지 갱신되고 있었다

### 이월

① ~~원격 push 여부(공개 저장소)~~ — `origin/main` `1ea9d81`로 해소 ② 09-20 제출 때 `.env` 전환(`HANDOFF.md` 절차, 과잉 사용 방지 한도·배포 보안 설정 복원 포함) ③ 임베딩 Gemini 무료 키 한도 확인 ④ planner `beats` 상한 강제(프롬프트 명시 또는 절단) ⑤ 어댑터 `response.usage` 기록으로 비용 실측 ⑥ Gemini 쿼터 리셋 후 80턴 재실행은 NPC 후보 제외로 불필요 ⑦ F11 원숭이손, 테스터7 F1·F2 ⑧ 배포 전 보안 설정 확인 1건(09-17 수정 완료) ⑨ 신의 질문 판정·본문 일관성(테스터7 F4·F6, 테스터9 F10) ⑩ 난이도 보조와 채점 엄격도 목표 점수대 결정(테스터8 F3·F4, 테스터9 F11 — 채점기는 A등급), motive-2 매칭 문장 지목 오류 ⑪ 낮 화면 재구성 묶음(테스터7 F2·F3·F7·F8, 테스터9 F4~F8·F12·F13) ⑫ NPC 화자 인식·대화 맥락(테스터8 F5, 테스터7 F5), 규칙 해석 실패 시 재입력 경로(테스터8 F6), 회차 전환 화면 밀림(테스터8 F1) ⑬ 로그인 안내 문구·랜딩 BGM(테스터9 F1·F3), 로컬 테스트 안내에 "Chrome + `http://localhost:3500`" 명시 검토(테스터9 F2) ⑭ 테스터9 판 종료 후 attempt 확정·기록 커밋
