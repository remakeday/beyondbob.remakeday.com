---
title: "Anthropic 키 교체 뒤 재부팅된 서버 복구, 새 키로 첫 판 시작 확인 (21:52~23:45, 코드 수정 없음)"
author: chungsik
tags: [개발일지]
---

사용자가 내일 만료되는 Anthropic 키를 `backend/.env`에서 새 키로 바꿨다(21:52). 확인해 보니 홈서버가 21:45에 재부팅돼 이 프로젝트 게임 DB(5435)와 백엔드(8500)가 둘 다 내려가 있었고, cloudflared 터널만 살아 있었다. 코드·설정 변경 없이 기동만 했다.

- **DB** — `docker compose up -d`로 게임 DB 재생성·healthy. alembic `current` = `heads`(216f290d03b3), public 테이블 16개 그대로.
- **백엔드** — `fuser -k 8500/tcp` 뒤 절대경로 uvicorn(`127.0.0.1:8500`, 로그 `backend/uvicorn-8500.log`, gitignore `*.log`). `get_settings()`: core anthropic claude-sonnet-5 · npc anthropic claude-haiku-4-5 · key set True · 과잉 사용 방지 허들 적용 — 09-18 제출 전환 구성 그대로.
- **새 키 검증** — 토큰 소모 없는 `models.list`로 인증 통과(키 값은 어디에도 적지 않음).
- **외부 주소** — `https://api.remakeday.com`: `/health` 200, `/docs` 404, 구글 시작 307, 비로그인 `POST /sessions` 401. 서버 로그 오류 0.
- **하지 않은 것** — 실제 Sonnet·Haiku 호출이 들어가는 판 플레이 스모크는 구글 로그인이 필요해 헤드리스로 못 돌렸다. 사용자가 한 판 열어 다음 장면·NPC 답변이 200이면 키 교체 완료. 재부팅 때 DB·백엔드가 자동으로 안 뜨는 문제는 손대지 않았다(다른 프로젝트 컨테이너도 같은 상태).

### 밤 — 새 키로 실제 플레이 확인 (23:45 일지 시점, 서버 로그 기준)

기동 뒤 `backend/uvicorn-8500.log`(23줄)에 구글 로그인 한 건이 지나갔다. 순서는 `auth/google/start` 307 → `google/callback` 307 → `POST /sessions` 200 → `POST /sessions/{id}/loops` 200 → `GET /loops/{id}/observations` 200 → `GET /loops/{id}/npcs` 200. uvicorn 접근 로그에는 시각이 없어 정확한 시간은 모른다(로그 마지막 수정 23:04, 그 뒤는 외부 스캐너의 `GET /` 404 두 건뿐).

- **Sonnet 확인됨** — 판 시작(`start_loop`)은 회차 행을 만든 직후 `_run_planner`를 부르는 구조라, `loops` 200은 새 키로 Sonnet planner 호출이 끝까지 성공했다는 뜻이다(`loop_interactor.py:182`).
- **Haiku 미확인** — NPC 대화(`POST /loops/{id}/utterances`)와 장면 진행(`beats/next`)은 로그에 없다. 첫 회차 아침까지만 열어 본 상태. Haiku 쪽은 사용자가 NPC에게 한 번 말을 걸어 200이 나오면 끝.
- **상태 재확인** — 백엔드 PID 25287 살아 있음, 게임 DB Up 2 hours (healthy), 로그에 5xx·traceback 0. 오늘 커밋 0건, 코드 변경 파일 0개(오늘 수정된 파일은 `backend/.env`·로그 2개·이 일지뿐). `model_evaluation.md`·`metrics.yml` 변경 없음.
- **이월** — 09-19 저녁의 그림 WebP 전환·선로딩(프론트 파일 96개 diff + WebP 75장 untracked)은 여전히 미커밋. 재부팅 자동 기동 문제도 그대로.
